name: among_us_offline
description: "A new Flutter project for Imposter Real-Life Offline."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # مكتبات الشبكة والكاميرا والـ QR وتسجيل جوجل
  network_info_plus: ^6.0.0
  mobile_scanner: ^5.0.0
  qr_flutter: ^4.1.0
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true